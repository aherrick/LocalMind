<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LocalMind.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:LocalMind.ViewModels"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <ScrollViewer Padding="16">
        <StackPanel Spacing="24" MaxWidth="520" HorizontalAlignment="Left">
            <TextBlock Text="Settings" Style="{ThemeResource TitleTextBlockStyle}" />

            <StackPanel Spacing="8">
                <TextBlock Text="Foundry Local" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <ItemsControl ItemsSource="{Binding FoundryModels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:FoundryModelVM">
                            <Grid Margin="0,4" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{x:Bind DisplayName}" />
                                <Button Grid.Column="1"
                                        MinWidth="96"
                                        Content="{x:Bind Status, Mode=OneWay}"
                                        IsEnabled="{x:Bind IsBusy, Mode=OneWay, Converter={StaticResource NotBoolConverter}}"
                                        Command="{x:Bind DownloadCommand}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBlock Text="Ollama" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock Text="{Binding OllamaStatus}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <ItemsControl ItemsSource="{Binding OllamaModels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock Margin="0,2" Text="{x:Bind}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
